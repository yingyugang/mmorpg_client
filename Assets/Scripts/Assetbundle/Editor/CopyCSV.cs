using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class CopyCSV : EditorWindow {

//	[MenuItem ("Tools/CopyCSV")] 
//	public static void Copy(){
//		string[] strs = FileManager.GetFiles (Application.dataPath + "/Animations","*.csv",System.IO.SearchOption.AllDirectories);
//		foreach(string str in strs){
//			Debug.Log ("str:" + str);
//			string fileName = str.Substring (str.LastIndexOf ('/'));
//			FileManager.CopyFile (str, Application.dataPath + "/CSV" + fileName);
//		}
//	}
}
